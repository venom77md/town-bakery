# Town Bakery - Environment Variables Template
# Copy this file to .env.local and update with your actual values

# Database Configuration
# SQLite database (local development)
DATABASE_URL="file:./prisma/dev.db"

# Optional: PostgreSQL (for production)
# DATABASE_URL="postgresql://user:password@localhost:5432/town_bakery?schema=public"

# Paymob Payment Gateway Configuration
# Get these from: https://accept.paymob.com
# For sandbox/testing: https://accept.paymobsolutions.com
PAYMOB_API_KEY=your_paymob_api_key
PAYMOB_INTEGRATION_ID=your_integration_id
PAYMOB_IFRAME_ID=your_iframe_id
PAYMOB_HMAC_SECRET=your_hmac_secret
PAYMOB_HMAC=your_hmac_secret  # Alternative name (same as PAYMOB_HMAC_SECRET)
# Set to 'true' to use sandbox/test environment
PAYMOB_SANDBOX=false
# Set to 'true' to use mock mode (no real API calls) - auto-enabled if API_KEY is empty
PAYMOB_MOCK_MODE=false

# Admin Authentication
NEXT_PUBLIC_ADMIN_PASSWORD=admin123

# Contact Information
NEXT_PUBLIC_CONTACT_PHONE=+201234567890

# Analytics (Optional)
NEXT_PUBLIC_GA_ID=your_google_analytics_id
NEXT_PUBLIC_META_PIXEL_ID=your_meta_pixel_id

# Supabase (Optional - Not required for local SQLite setup)
# NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Node Environment
NODE_ENV=development

