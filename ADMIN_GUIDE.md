# 🔐 دليل لوحة التحكم - Town Bakery

## 🚀 الوصول إلى لوحة التحكم

### خطوات تسجيل الدخول:

1. اذهب إلى `/admin/login` أو `/admin` (سيتم التوجيه تلقائياً)
2. أدخل كلمة المرور:
   - **الافتراضية**: `admin123`
   - **للتغيير**: عدّل `NEXT_PUBLIC_ADMIN_PASSWORD` في `.env.local`
3. اضغط "دخول"

---

## 📋 صفحات لوحة التحكم

### 1. `/admin/dashboard/orders` - إدارة الطلبات

**المميزات:**
- ✅ عرض جميع الطلبات
- ✅ تصفية حسب الحالة (الكل، جديدة، قيد التحضير، تم التوصيل)
- ✅ تحديث حالة الطلب مباشرة
- ✅ عرض معلومات العميل والدفع

**كيفية الاستخدام:**
1. اختر الفلتر المطلوب من الأزرار العلوية
2. لتغيير حالة الطلب: اختر من القائمة المنسدلة
3. التغييرات تحفظ تلقائياً

---

### 2. `/admin/dashboard/products` - إدارة المنتجات

**المميزات:**
- ✅ عرض جميع المنتجات
- ✅ إضافة منتج جديد
- ✅ تعديل منتج موجود
- ✅ حذف منتج (قيد التطوير)

**إضافة منتج جديد:**
1. اضغط "إضافة منتج جديد"
2. املأ البيانات:
   - الاسم بالعربي (مطلوب)
   - الاسم بالإنجليزي (اختياري)
   - الوصف بالعربي (مطلوب)
   - السعر (EGP)
   - الفئة (حلويات، معجنات، خبز)
   - رابط الصورة (مثل `/assets/product-1.jpg`)
3. اضغط "إضافة المنتج"

**تعديل منتج:**
1. اضغط "تعديل" على المنتج المطلوب
2. عدّل البيانات
3. اضغط "حفظ التعديلات"

---

### 3. `/admin/dashboard/messages` - الرسائل

**المميزات:**
- ✅ عرض جميع رسائل الاتصال
- ✅ عرض تفاصيل الرسالة الكاملة
- ✅ تنظيم حسب التاريخ

**كيفية الاستخدام:**
1. اضغط على أي رسالة لعرض التفاصيل الكاملة
2. في الجانب الأيمن سيظهر:
   - اسم المرسل
   - البريد الإلكتروني
   - تاريخ الإرسال
   - نص الرسالة الكامل

---

## 🔒 الأمان

### تغيير كلمة المرور:

1. افتح `.env.local`
2. عدّل `NEXT_PUBLIC_ADMIN_PASSWORD=your-new-password`
3. أعد تشغيل السيرفر

### حماية الجلسة:

- الجلسة تنتهي بعد 24 ساعة تلقائياً
- يمكن تسجيل الخروج يدوياً
- عند انتهاء الجلسة، يجب تسجيل الدخول مرة أخرى

---

## ⚠️ ملاحظات مهمة

1. **لا تستخدم كلمة المرور الافتراضية في الإنتاج**
2. **استخدم كلمات مرور قوية**
3. **في الإنتاج، استخدم نظام authentication مناسب (Supabase Auth)**
4. **احفظ نسخة احتياطية من البيانات بانتظام**

---

## 🐛 حل المشاكل

### لا يمكن تسجيل الدخول:
- تأكد من أن `NEXT_PUBLIC_ADMIN_PASSWORD` مضبوطة بشكل صحيح
- تأكد من أن الجلسة السابقة منتهية (امسح sessionStorage)

### البيانات لا تظهر:
- تأكد من أن Supabase متصل بشكل صحيح
- تحقق من أن `DATABASE_URL` صحيح
- شغّل `npm run prisma:generate` إذا لزم الأمر

### لا يمكن تحديث الطلبات:
- تحقق من أن `/api/orders` route يعمل
- تأكد من أن قاعدة البيانات متصلة

---

## 📞 الدعم

للمساعدة الإضافية، راجع:
- `SETUP_INSTRUCTIONS.md` - إعداد Supabase
- `README.md` - معلومات عامة عن المشروع

---

**آخر تحديث**: 2025-11-02

