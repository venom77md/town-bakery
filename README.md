# Town Bakery - مخبز تاون

موقع إلكتروني متكامل لمخبز تاون مع دعم كامل للغة العربية (RTL)، تكامل Supabase للبيانات، ودفع آمن عبر Paymob.

## ✨ المميزات

- ✅ **Next.js 14** مع App Router
- ✅ **Supabase + Prisma** لإدارة قاعدة البيانات
- ✅ **Paymob** لمعالجة المدفوعات
- ✅ **RTL Support** - دعم كامل للعربية
- ✅ **Responsive Design** - يعمل على جميع الأجهزة
- ✅ **Framer Motion** - تحريكات سلسة
- ✅ **SEO Optimized** - محسّن لمحركات البحث
- ✅ **TypeScript** - نوعية آمنة للكود

## 🚀 البدء السريع

### 1. تثبيت المكتبات

```bash
npm install
```

### 2. إعداد Supabase

اتبع الإرشادات في [`SETUP_INSTRUCTIONS.md`](./SETUP_INSTRUCTIONS.md) لإنشاء مشروع Supabase والحصول على بيانات الاتصال.

### 3. إعداد Paymob

اتبع الإرشادات في [`SETUP_INSTRUCTIONS.md`](./SETUP_INSTRUCTIONS.md) لإعداد حساب Paymob (Sandbox).

### 4. إعداد ملف البيئة

```bash
cp .env.example .env.local
```

املأ جميع القيم في `.env.local` (راجع `SETUP_INSTRUCTIONS.md`).

### 5. إعداد قاعدة البيانات

```bash
npm run db:setup
```

هذا الأمر سيقوم بـ:
- توليد Prisma Client
- إنشاء جداول قاعدة البيانات
- إضافة المنتجات الأولية

### 6. تشغيل المشروع

```bash
npm run dev
```

افتح http://localhost:3000

## 📦 الأوامر المتاحة

```bash
# التطوير
npm run dev              # تشغيل السيرفر المحلي
npm run build            # بناء للإنتاج
npm run start            # تشغيل نسخة الإنتاج

# قاعدة البيانات
npm run prisma:generate  # توليد Prisma Client
npm run prisma:migrate   # إنشاء Migration جديد
npm run prisma:seed      # إضافة بيانات أولية
npm run prisma:studio    # فتح Prisma Studio
npm run db:setup         # إعداد كامل للقاعدة

# الأدوات
npm run download-assets  # تحميل الصور
npm run extract-color    # استخراج اللون من الشعار
```

## 🗂️ هيكل المشروع

```
town-bakery/
├── app/                    # صفحات Next.js
│   ├── (components)/       # المكونات المشتركة
│   ├── api/                # API Routes
│   │   ├── orders/         # إدارة الطلبات
│   │   ├── products/       # المنتجات
│   │   ├── contact/        # نموذج الاتصال
│   │   └── payments/       # تكامل Paymob
│   ├── home/               # الصفحة الرئيسية
│   ├── products/           # قائمة المنتجات
│   ├── cart/               # السلة
│   ├── admin/              # لوحة التحكم
│   └── ...
├── lib/                    # مكتبات مساعدة
│   ├── prisma.ts           # Prisma Client
│   ├── supabase.ts         # Supabase Client
│   ├── paymob.ts           # دالات Paymob
│   └── products.ts         # بيانات المنتجات
├── prisma/
│   ├── schema.prisma       # مخطط قاعدة البيانات
│   └── migrations/         # Migrations
├── public/
│   └── assets/             # الصور والملفات الثابتة
└── scripts/                # سكريبتات مساعدة
```

## 🔐 الأمان

- **Admin Password**: افتراضي `admin123` - غيّره في `.env.local`
- **API Keys**: لا تشارك مفاتيح Supabase أو Paymob
- **Environment Variables**: `.env.local` موجود في `.gitignore`

## 🌐 النشر

### Vercel (موصى به)

1. ادفع الكود إلى GitHub
2. اربط المشروع في Vercel
3. أضف متغيرات البيئة في Vercel Dashboard
4. Vercel سيقوم بالنشر تلقائياً

### متغيرات البيئة في Vercel:

أضف جميع المتغيرات من `.env.local` في:
**Settings** → **Environment Variables**

## 📞 الدعم

للمساعدة، راجع:
- [`SETUP_INSTRUCTIONS.md`](./SETUP_INSTRUCTIONS.md) - إرشادات الإعداد
- [`LOG_AR.md`](./LOG_AR.md) - سجل العمليات

## 📄 الترخيص

هذا المشروع مخصص للاستخدام التجاري لمخبز تاون.

---

**صُنع بـ ❤️ لمخبز تاون**
